# --- mirrored from bpm-rulepacks (public-teaser subset) ---
# Source of truth: bpm-rulepacks/packs/secrets/hardcoded-credentials.yaml (proprietary corpus,
# private repo). That file contains TWO rules; only bpm-secret-aws-access-key is tagged
# metadata.license: public-teaser. The sibling rule (bpm-secret-generic-assignment) is
# metadata.license: proprietary and is intentionally NOT mirrored here -- do not copy it.
# MIT-licensed here; do not edit in place if bpm-rulepacks later revises the source rule --
# re-copy instead. See rules/README.md for the resync procedure.
rules:
  - id: bpm-secret-aws-access-key
    languages: [generic]
    severity: ERROR
    message: >
      Hardcoded AWS access key ID. Move to a secrets manager or environment
      variable; rotate the exposed key immediately.
    pattern-regex: \bAKIA[0-9A-Z]{16}\b
    metadata:
      license: public-teaser
      provenance: ported:slop-scorecard/checks/secrets.ts
      cwe: [798]
      owasp: "A07:2021"
      confidence: high
      concept: secrets_config
      since: "0.1.0"
